query getBandBySlug($slug: String!) {
  getBandBySlug(slug: $slug) {
    name
    bio
    photos {
      src
      width
      height
    }
    albums {
      cover
      title
      year
      songs {
        id
        audio
        length
        title
      }
    }
  }
}
