mutation multipleUpload($bandId: Int!, $files: [Upload!]!, $uploadFolder: String!, $dbModel: String!) {
  multipleUpload(bandId: $bandId, files: $files, uploadFolder: $uploadFolder, dbModel: $dbModel) {
    id
  }
}
